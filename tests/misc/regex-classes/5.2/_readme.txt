The files in this directory represent expected results for a processor
using Unicode 5.2. They were submitted by Oliver Hallam oliver@cbcl.co.uk.

For a processor using a different Unicode version, add another version of the
results in a different directory. The stylesheet itself checks all relevant
versions: it does not depend on the test driver to do so.

Michael Kay, 24 June 2010